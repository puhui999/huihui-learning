spring:
  application:
    name: demo-00-hello
  ai:
    openai:
      # DeepSeek 提供 OpenAI 兼容接口；Key 只从环境变量读，绝不写死在文件里
      base-url: https://api.deepseek.com
      api-key: ${DEEPSEEK_API_KEY}
      chat:
        model: deepseek-chat
        temperature: 0.7
